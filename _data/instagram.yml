# Instagram feed for the "Uudised" section.
#
# The feed is fetched in the browser from Behold's public JSON endpoint, so it
# is always current: nothing here is baked at build time and no rebuild is
# needed when the club posts. Behold holds the Instagram token and refreshes
# it, so there is nothing that expires.
#
# The feed id is public by design - it appears in the image URLs the feed
# returns - so it lives here rather than in an environment variable.

enabled: true
feed_id: IVLyWbovlsdroEMms5FG
post_count: 3
username: taltechbasketballschool
profile_url: https://www.instagram.com/taltechbasketballschool/
